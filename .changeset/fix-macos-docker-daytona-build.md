---
"@ai-hero/sandcastle": patch
---

Fix macOS Docker image builds when the host group id already exists in the base image, and install Daytona's SDK as a dev dependency so source builds can resolve the optional provider's types.
